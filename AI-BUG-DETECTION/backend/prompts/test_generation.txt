Generate a unit test in {language} that validates the fix for this bug.
The original code contains the bug, and the fixed code corrects it.
Write a test that reproduces the bug on the original code, and asserts success on the fixed code.

Original Code:
```
{code}
```

Fixed Code:
```
{fixed_code}
```

Return structured JSON only:
{{
  "test_case": "unit test code logic"
}}
